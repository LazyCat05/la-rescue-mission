<h1><%= @question.title %></h1>
<p><%= @question.description %></p>

<%= form_for [@question, @answer] do |f| %>
  <div>
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
